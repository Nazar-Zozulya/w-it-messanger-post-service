model Post {
    id      Int     @id @default(autoincrement())
    title   String
    content String?
    authorId Int
    author   User @relation("PostsAuthored", fields: [authorId], references: [id])

}